fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets>=12.0
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.27
openai>=1.30
numpy>=1.26
pandas>=2.1
redis>=5.0
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0
alembic>=1.13
python-dotenv>=1.0
rich>=13.7
loguru>=0.7.2
aiohttp>=3.9
prometheus-client>=0.20
python-multipart>=0.0.9  # Form data support
nats-py>=2.7  # NATS JetStream client

# ── REMOVED: Heavy ML packages (saved ~4.8 GB) ──────────
# These are imported lazily (try/except) by brains — not needed at startup.
# Brains gracefully degrade (return neutral signals) when deps are missing.
# To enable, install in a separate training container or add back:
#   stable-baselines3>=2.3  # PPO/SAC — pulls torch+nvidia (~4.5 GB)
#   gymnasium>=0.29         # RL environments (used by finrl_brain)
#   scikit-learn>=1.5       # ML (used by ml_signals.py)
#   qdrant-client>=1.9      # Vector DB (used by memory/vector_memory.py)
#   pandas-ta==0.4.71b0    # Technical indicators (used by ml_signals.py)
#   optuna>=3.6             # Bayesian optimization (used by strategy_evolver)
#   torch>=2.0              # GPU inference (used by custom_nn_brain, deep_encoder)

