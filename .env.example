# ── Inference ─────────────────────────────────────────────────
NVIDIA_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=
OPENAI_API_KEY=

# ── App defaults ───────────────────────────────────────────────
INITIAL_BALANCE=1000.0
INFERENCE_BUDGET_TIER=free
APP_MODE=paper

# ── Binance (optional — public data endpoints do not require keys) ──
# BINANCE_API_KEY=
# BINANCE_SECRET=

# Note: keep real keys in a local .env that is git-ignored.
# This file is only a template.
