# Production Remediation State

Updated: 2026-07-24 (Asia/Ulaanbaatar)  
Branch: `fix/production-ready-paper-runtime`  
Baseline commit: `0139367ceb05`  
Release boundary: paper/replay only. Live and mainnet execution remain prohibited.

`VERIFIED` means a current source or runtime check passed. `FIXED_UNVERIFIED`
means the local control exists but a required duration or external delivery proof
does not. No legacy container, network, or volume was deleted.

| ID | Status | Confirmed root cause and implemented remediation | Verification / remaining risk | Rollback |
|---|---|---|---|---|
| AUD-001 | VERIFIED | The 23-container legacy project was stopped and preserved. A separate 12-service `trade_agent_canonical` paper graph now runs the manifest-derived workers, canonical PostgreSQL, NATS, Redis, and read-only control plane. Containers use loopback ports, read-only roots, dropped capabilities, `no-new-privileges`, PID limits, and bounded logs. | Runtime is `ready=true`, `mode=paper_live`; 7 leases are ready. Legacy remains stopped, not removed. | Stop only `trade_agent_canonical`; use the recorded legacy `docker start` command only for an approved rollback. |
| AUD-002 | VERIFIED | The legacy surface had 34 anonymous mutations. It is unreachable because every legacy container is stopped. The canonical HTTP application contains GET-only health/readiness/runtime/metrics routes and exposes only loopback `8001`. | OpenAPI contract rejects POST/PUT/PATCH/DELETE routes; no anonymous mutation was invoked. | Recreating the legacy API reintroduces the finding and is not an approved release rollback. |
| AUD-003 | VERIFIED | Chroma 1.5.9 has no stable patched release for CVE-2026-45829. It was removed from the default trading graph, placed behind the explicit `knowledge` profile on its own network, bound to loopback, and given `restart: "no"`. | Default 12-service graph excludes Chroma. The vulnerable image must remain disabled pending a supported patched release. | Do not attach Chroma to the trading network. |
| AUD-004 | VERIFIED | The broad duplicate `.env` contract was replaced for canonical use by one generated ignored env plus two independent read-only DB secret files. Owner/admin/runtime roles are separated; paper containers receive no broker/cloud/provider secret names. Windows ACLs allow only owner, Administrators, and SYSTEM. | Redacting preflight passes; container env/mount inspection passed without values. | Preserve the generated files; never fall back to the legacy writable `.env` mount. |
| AUD-005 | BLOCKED_EXTERNAL | Local prevention is implemented through secret-file scope and redacting CI scanning. Historical TLS key compromise cannot be remediated locally. | CA revocation/reissue and sanitized certificate proof are absent. | Never restore the compromised key. |
| AUD-006 | FIXED_UNVERIFIED | vLLM was removed from the default graph and placed in the explicit `gpu` profile with `restart: "no"` and a pinned digest. Ollama is the explicit default local provider. | The legacy 79-restart loop is contained. GPU admission and a full 24-hour vLLM soak are not verified. | Keep the GPU profile disabled. |
| AUD-007 | VERIFIED | The broken legacy feed was retired from the release path. The canonical Binance public WebSocket producer handles only closed BTC/ETH 1m candles, typed identities, deduplication, bounded reconnects, quality gates, and freshness checks. Two production bugs were fixed: JSONB checkpoint canonicalization and accepted clock-skew evaluation time. | Real public flow reached 74 raw/74 validated events and feature state advanced to 10+ candles before capture. | Disable `market-producer` and use deterministic replay if public data becomes unavailable. |
| AUD-008 | VERIFIED | Fresh named volumes were provisioned without touching legacy state. Checksummed migrations 001-007 ran under the owner identity; workers use a distinct runtime role with all elevated flags false and no migration-history write grants. | 7 exact migrations, active policy, public Binance constraints, inbox/outbox, leases, decisions, intents, fills, reconciliation, and kill-switch tables were queried. | Preserve both canonical and legacy volumes; restore only into a new volume. |
| AUD-009 | VERIFIED | `scripts/volume_backup.py` implements stopped-volume archive backup, AES-256-GCM streaming encryption, separate key material, hashes, labels, and restore-to-new-volume-only. Legacy PostgreSQL and NATS were backed up and restored into isolated volumes. | PostgreSQL restored with expected DB/table/login counts; NATS restored with expected 1 stream/0 messages. Off-host immutable retention/KMS remains external. | Restores must target a new labeled volume; never overwrite active state. |
| AUD-010 | VERIFIED | Missing Prometheus rule mounts were added. Liveness, dependency readiness, and paper execution enablement are separate. NATS outage fault injection made readiness and execution false, then recovered. Feature freshness and all seven renewable worker leases are part of readiness. A transient lease-write timeout no longer leaves producer health permanently degraded. | `promtool`: config valid, 13 alert rules, 20 recording rules. Runtime recovered to HTTP 200 readiness with 7/7 fresh leases and no stale worker errors. Actual receiver delivery is not claimed. | Revert probe/rule changes together only if monitoring cannot start. |
| AUD-011 | VERIFIED | `DB_RUNTIME_PASSWORD` is documented as a secret-file contract; inline DB secrets are rejected. A one-shot initializer and redacting preflight validate duplicates, placeholders, malformed/remote URLs, live mode, and scope. | `.local/canonical.env` preflight reports zero findings; default Compose renders 12 services. | Regenerate in a new path; initializer never overwrites secrets. |
| AUD-012 | VERIFIED | The migration runner verifies immutable 001-007 names/hashes. CI runs on every push/PR with pinned action SHAs. Manifest coverage and ownership were updated. | Full Python suite passed; knowledge lock/check must remain green after every change. | Update manifest and lock atomically. |
| AUD-013 | VERIFIED | Canonical deployable scope is explicit and clean; legacy/research code stays profile-gated and non-release. | Ruff passed `packages workers`; MyPy passed 117 source files with explicit package bases. Broader legacy test lint still has 25 pre-existing findings. | Do not expand the release graph to legacy services. |
| AUD-014 | VERIFIED | Deterministic replay remains model-free. A supervised, explicit `deterministic_baseline` acceptance provider was added solely to prove real public-feed plumbing without coercing the LLM; deterministic risk remained authoritative and default `ollama` was restored afterward. | Real flow: 6 candidates, 6 decisions (2 approved/4 rejected), 2 paper intents, 2 simulated fills, 0 reconciliation mismatches, 0 duplicate inbox/fills. | Recreate candidate worker with `CANDIDATE_PROVIDER=ollama` (current state). |
| AUD-015 | VERIFIED | Base/service images are digest-pinned and GitHub Actions are commit-SHA pinned. CI image tags use the commit SHA and request SBOM plus maximum provenance attestations. | Compose/image policy tests pass. Local Compose build aliases are not release artifacts. | Revert a pin only with reviewed advisory and compatibility evidence. |
| AUD-016 | TODO | Existing Kubernetes/Terraform topology has not yet been rebuilt from the canonical graph. | No cloud apply was authorized; Terraform is unavailable locally. | Source-only work; never apply infrastructure without authorization. |
| AUD-017 | VERIFIED | Canonical services use bounded `json-file` rotation (`10m`, 5 files); monitoring defines disk/restart safety rules. Reconciliation now persists a sanitized failure, activates the kill switch, marks its lease unhealthy, and retries instead of crash-looping on a transient public quote timeout. | Runtime inspect confirmed bounded options. The remediated reconciliation container stayed running at restart count 0 while fresh matched projections resumed. Alert delivery remains unverified. | Preserve log bounds during future Compose edits. |
| AUD-018 | FIXED_UNVERIFIED | Executable legacy model paths are absent from the canonical paper graph; candidate artifacts are digest-bound and unknown provenance is rejected. | Full safe-format migration of every tracked research model is incomplete. | Keep legacy binary models quarantined/read-only. |
| AUD-019 | IN_PROGRESS | Critical Chroma advisory was checked against the primary GitHub advisory; npm audit is clean; digests, SBOM, and provenance gates were added. | A reviewed expiring VEX inventory and all-language dependency scan remain incomplete. | Do not waive vulnerabilities without owner, rationale, and expiry. |
| AUD-020 | FIXED_UNVERIFIED | Truthful readiness, NATS fault recovery, lease/freshness gates, correlated event IDs, bounded metrics, and rule validation are implemented. | External alert delivery and a completed 24-hour fault soak are not proven. | Keep paper execution fail-closed on readiness loss. |
| AUD-021 | FIXED_UNVERIFIED | Candidate events bind provider, model/strategy identity, artifact digest, market snapshot, candidate hash, and risk verdict. Default Ollama and supervised deterministic baseline provenance cannot be relabeled. | A complete model registry with calibration/expiry/rollback metadata is still incomplete. | Unknown or tampered provenance must continue to fail validation. |
| AUD-022 | IN_PROGRESS | CI includes Python, frontend, Go, Compose, security, knowledge, SBOM/provenance, and Playwright gates. Go test/vet pass, but the Go module has no behavioral tests. | Rust/Cargo are unavailable; Terraform is unavailable; 24h/7d soaks are incomplete. | Do not mark unavailable gates green. |
| AUD-023 | VERIFIED | User-owned `frontend/next-env.d.ts` and `package-lock.json` were preserved. Frontend gates ran from an isolated Git archive. | Lint 0 errors/7 allowed warnings, typecheck/build/audit pass, Playwright 14/20 parallel plus all 7 microstructure tests serial pass. Parallel flakiness remains. | Delete only the disposable ignored validation copy after path verification. |
| AUD-024 | VERIFIED | Every governed path has an owner/lifecycle classification, including remediation, evidence, and new candidate provider files. | Knowledge validate/check is the release gate. | Regenerate the lock after manifest-governed changes. |
| AUD-025 | TODO | Runtime DBs remain outside the new canonical source graph, but full signed external artifact storage and removal of all tracked binary models are not implemented. | Requires an approved artifact repository/retention owner. | Do not delete tracked artifacts until recipes and checksums reproduce them. |
| AUD-026 | TODO | Edge CSP/banner/Grafana plugin hardening was not completed because the legacy edge is quarantined and not in the canonical release graph. | Required before any frontend/edge production exposure. | Keep the edge profile disabled and loopback-only. |

## Current release decision

The canonical local paper runtime is operational and materially remediated, but
the repository is **not production/live approved**. The next dependency gates are
AUD-016, AUD-019, AUD-021/022, AUD-025/026, external certificate/off-host backup
actions, and the full 24-hour then seven-day paper soaks.
